{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/glory/Desktop/SSAju/ssaju-frontend && npm run build > /tmp/ssaju-build.log 2>&1 && echo '✅ Build passed' || echo '❌ Build failed — 오류 확인: cat /tmp/ssaju-build.log'",
            "timeout": 120,
            "statusMessage": "🔨 빌드 검증 중...",
            "async": true
          }
        ]
      }
    ]
  }
}
